CREATE TABLE DIJKSTRA_LOG (
  ID NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  RUN_AT TIMESTAMP DEFAULT SYSTIMESTAMP,
  SOURCE_NODE VARCHAR2(50),
  TARGET_NODE VARCHAR2(50),
  PATH VARCHAR2(4000),
  DISTANCE NUMBER,
  DETAILS CLOB
);

COMMIT;